# Directory Guide

## Purpose
Income UI components.

## Key files
- `IncomeForm.jsx`: create flow.
- `IncomesList.jsx`: table/list, inline editing, delete, undo/redo triggers.

## Extension notes
- Reuse `src/api/incomes.js` and `src/api/undo.js` for consistent behavior.