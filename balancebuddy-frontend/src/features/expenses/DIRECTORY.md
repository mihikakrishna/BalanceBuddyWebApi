# Directory Guide

## Purpose
Expense UI components.

## Key files
- `ExpenseForm.jsx`: create flow.
- `ExpensesList.jsx`: table/list, inline editing, delete, undo/redo triggers.

## Extension notes
- Reuse `src/api/expenses.js` and avoid duplicating request logic in components.